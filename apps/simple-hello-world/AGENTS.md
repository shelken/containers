# simple-hello-world 应用规则

- 这是一个无后端的 fancy landing page，只保留前端静态页面能力。
- 页面风格走精致玻璃质感、微交互、高级留白和强动效，不添加后端、数据库、密钥或持久化资源。
- 复杂滚动与入场动效使用 GSAP；普通 hover 微交互优先使用 Tailwind transition。
- 修改后至少运行 `npm run build`；需要本地容器预览时运行 `npm run docker:smoke`。
